# gallery/ — rendered glyph "tablets" (§0.6, F2.3)

Generated corpora rendered as SVG tablets with invented sign forms. Play is licensed and bounded:

- Every gallery session declares a **timebox in PLAN (§4.3) before it starts** and reports the time
  consumed in HANDOFF's `DRIFT CHECK`. Gates demand ROC artifacts, not screenshots (§5).
- Every rendered tablet is derived from a manifested corpus and lands here **with a manifest like
  any corpus** (F2.3): the manifest names the source `corpus_id` and `content_hash`, so a tablet is
  never an orphan image.
- Gallery output has no result authority. Nothing here enters CLAIMS.md.

Empty until F2.3.
